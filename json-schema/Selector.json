{
  "schema": "http://json-schema.org/draft-04/schema#",
  "id": "Selector",
  "metaFormat": "http://purl.imsglobal.org/caliper/",
  "type": "object",
  "properties": {},
  "title": "Selector",
  "description": "A Caliper Selector is a generic class that describes a fragment or segment of a resource.  Utilize Selector only if no suitable subtype exists to represent the selection being described.",
  "translations": {
    "title": [
      {
        "en": "Selector"
      }
    ],
    "description": [
      {
        "en": "A Caliper Selector is a generic class that describes a fragment or segment of a resource.  Utilize Selector only if no suitable subtype exists to represent the selection being described."
      }
    ]
  }
}